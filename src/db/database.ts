/** @deprecated Use get* actions / CMS. Kept for seed generator compatibility. */
export {
	FALLBACK_ENTERTAINMENTS as entertainments,
	FALLBACK_PROGRAM as program,
	FALLBACK_TOURS as tours
} from "@/lib/contentFallback";
